services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    command: sleep infinity
    env_file:
      - .env

  hardhat:
    build:
      context: ./hardhat
      dockerfile: Dockerfile
    network_mode: service:app
  
  block-explorer:
    build:
      context: ./block-explorer
      dockerfile: Dockerfile
    network_mode: service:app